// Main Components Index - Legacy components remaining after migration

// Utility Components
export { ErrorBoundary } from './errorBoundary';
export { ClientOnly } from './clientOnly';

// Dashboard Components (still in legacy location)
export { DashboardLayout } from './dashboard/DashboardLayout';
export { MetricsCard } from './dashboard/MetricsCard';

// Note: Most components have been migrated to src/features/
// Use @/src/features/ for new components
